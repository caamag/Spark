import * as _ from "./style";
import { VscLoading } from "react-icons/vsc";

const Loading = () => {
  return (
    <_.LoadingContainer>
      <VscLoading />
    </_.LoadingContainer>
  );
};

export default Loading;
